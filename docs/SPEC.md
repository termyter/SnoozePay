# ТЕХНИЧЕСКОЕ ЗАДАНИЕ
# Мобильное приложение-будильник с платным откладыванием

**Версия:** 2.1
**Дата обновления:** 2026-06-12
**Статус:** Активная разработка

---

## ⚠️ ПРИОРИТЕТ ИСТОЧНИКОВ (2026-06-12)

**Дизайн-канон побеждает это ТЗ.** Единственный источник истины по продуктовым и UI-решениям — дизайн-хэндофф `docs/design/v2-handoff/` (JSX-прототипы + транскрипты `chats/chat1.md`–`chat7.md`). При любом противоречии между этим документом и хэндоффом действует хэндофф (внутри чатов побеждает более позднее решение). Это ТЗ актуализировано по канону 2026-06-12; изменённые секции помечены «(обновлено 2026-06-12 по дизайн-канону)». Свод расхождений: `docs/AUDIT_DESIGN_GAP_2026-06-12.md`.

Ключевые решения канона (детали в соответствующих секциях ниже):
- Цена откладывания: свободный ввод, минимум 1 ₽, максимума нет (chat3.md:1325-1333)
- Прогрессив с потолком: 4 ступени, максимум base×8 (#298)
- Длительность откладывания: per-alarm слайдер 1–15 мин (chat1.md:976,996)
- Табы: Будильники / Кошелёк / Статистика + шестерёнка → Настройки (chat3.md:1635-1750)
- Статистика только поведенческая, без денег (chat4.md:262-268)
- One-way stake: вывода средств нет (chat3.md:196-269)
- Словарь: везде «Баланс», жаргон «снуз» запрещён в продакшн-копи (chat1.md:986-998)

---

## ⚠️ КРИТИЧЕСКИЕ ИЗМЕНЕНИЯ (2026-02-24)

### Убрано из MVP:
- ❌ Backend сервер (Node.js/Python + PostgreSQL)
- ❌ Авторизация/логин через API
- ❌ API эндпоинты (`/api/auth/register`, `/api/auth/login`, etc.)
- ❌ Синхронизация с сервером

### Добавлено в MVP:
- ✅ **Локальное хранение баланса** (UserDefaults на iOS, SharedPreferences на Android)
- ✅ **Локальное хранение транзакций** (SQLite/Core Data)
- ✅ **Верификация IAP напрямую** — StoreKit 2 (iOS) и Google Play Billing (Android) имеют встроенную защиту
- ✅ **Offline-first подход** — приложение работает без интернета

### Перенесено в Версию 2.0:
- ➡️ Backend сервер для синхронизации
- ➡️ Авторизация (device_id или email)
- ➡️ Облачный бэкап будильников
- ➡️ Синхронизация баланса между устройствами

### Обоснование:
1. **Упрощение MVP** — быстрее выйти на рынок
2. **Снижение затрат** — нет серверов, нет infrastructure costs
3. **Надёжность** — нет зависимости от сервера при срабатывании будильника
4. **StoreKit 2 / Google Play Billing** — уже включают защиту от мошенничества

---

## 1. Общее описание проекта

### 1.1. Концепция *(обновлено 2026-06-12 по дизайн-канону)*
Мобильное приложение-будильник для iOS и Android с уникальной механикой: при нажатии кнопки «Поспать ещё» с баланса пользователя списывается денежная сумма. Базовую цену откладывания пользователь выбирает сам: минимум 1 ₽, максимума нет (chat3.md:1325-1333). Доступна прогрессивная шкала, при которой каждое последующее откладывание стоит дороже (с потолком — см. 2.2.3). Если баланс равен нулю, откладывание недоступно: будильник продолжает звонить, карточка «Поспать ещё» отключена, доступны кнопки «Пополнить» и «Я встал» (см. 2.2.2).

**Цель приложения** — помочь людям бороться с привычкой бесконечно откладывать будильник, используя финансовый стимул.

### 1.2. Целевая аудитория
- Люди, которые систематически откладывают будильник и опаздывают
- Возраст: 18–45 лет (основная группа)
- Студенты, офисные работники, фрилансеры
- Люди, мотивированные на самодисциплину и продуктивность
- Пользователи, готовые использовать финансовую мотивацию для изменения привычек

### 1.3. Проблема
Стандартные будильники позволяют бесконечно откладывать сигнал без каких-либо последствий. Это приводит к опозданиям, нарушению режима сна и снижению продуктивности. Существующие решения (задачки для выключения, QR-коды) работают на выключение, но не на откладывание. Финансовый штраф создаёт реальные последствия за каждое откладывание.

### 1.4. Монетизация (куда уходят штрафы)
- **MVP (первый этап):** средства поступают разработчику
- **Второй этап (будущее):** возможность переводить штрафы на благотворительность

### 1.5. Платформы и технологии

| Параметр | MVP (v1.0) | Версия 2.0 |
|----------|------------|------------|
| **iOS** | Swift, UIKit, AlarmKit (iOS 26+) / Local Notifications (iOS 18+), StoreKit 2, UserDefaults | + Backend sync, CloudKit |
| **Android** | Kotlin, Jetpack Compose, AlarmManager, Google Play Billing, SharedPreferences | + Backend sync |
| **Backend** | ❌ Нет (offline-first) | ✅ Node.js/Python + PostgreSQL |
| **Минимальная iOS** | iOS 18 (Local Notifications), iOS 26 (AlarmKit — полноценный будильник) | iOS 18+ |
| **Минимальная Android** | Android 8.0 (API 26) | Android 8.0+ |
| **Архитектура** | MVVM, Offline-first | MVVM + Cloud sync |
| **Дизайн** | Минималистичный, тёмная/светлая тема | + Виджеты, расширенная статистика |

---

## 2. Функциональные требования

### 2.1. Управление будильниками

#### 2.1.1. Создание будильника *(обновлено 2026-06-12 по дизайн-канону)*
- Выбор времени (часы, минуты) — блок «Подъём» с крупным clock-XL дисплеем
- Повтор: режимы «Никогда» (one-shot, после срабатывания будильник автоматически выключается) и «Еженедельно» — дни недели Пн–Вс, множественный выбор (реализовано, #229)
- Название будильника (текстовое поле, например: «Работа», «Спортзал», «Учёба»)
- Выбор звука будильника (из предустановленных бесплатных звуков)
- Выбор темы экрана срабатывания (6 готовых тем + своё фото)
- Включение/выключение вибрации
- Длительность откладывания — per-alarm слайдер, по умолчанию 9 минут, диапазон 1–15 минут (chat1.md:976,996; реализовано, диапазон в модели чинится в #286)
- Цена откладывания — свободный числовой ввод: минимум 1 ₽, максимума нет, чипы-подсказки 20/50/100/200/500 для быстрого заполнения, по умолчанию 50 ₽ (chat3.md:1196-1230, 1325-1333). **Целевое состояние** per #230; текущая реализация — только preset-чипы 20/50/100/200/500
- Включение/выключение прогрессивной шкалы

#### 2.1.2. Список будильников
- Отображение всех будильников в виде списка
- Каждый будильник показывает: время, дни повтора, название, сумму штрафа, статус (вкл/выкл)
- Toggle-переключатель для включения/выключения каждого будильника
- Свайп для удаления будильника
- Нажатие на будильник открывает экран редактирования

#### 2.1.3. Редактирование будильника
Все параметры из создания доступны для редактирования. Изменения применяются после нажатия «Сохранить».

#### 2.1.4. Длительность откладывания — per-alarm *(обновлено 2026-06-12 по дизайн-канону)*
~~Глобальная настройка времени откладывания~~ — **отменено каноном**. Длительность задаётся индивидуально для каждого будильника слайдером 1–15 минут прямо на экране создания/редактирования (chat1.md:976,996). Глобального дефолта в настройках нет. Реализовано.

### 2.2. Экран сработавшего будильника

#### 2.2.1. Основной экран *(обновлено 2026-06-12 по дизайн-канону)*
- Полноэкранный интерфейс (поверх Lock Screen, если возможно), тематизированный (6 тем + своё фото)
- Отображение текущего времени крупным моноширинным шрифтом
- Шапка: дата + пилюля баланса; название будильника
- Два действия (словарь без жаргона «снуз»):
  - **«Поспать ещё N мин»** — платная карточка с ценой: «−X ₽»
  - **«Я встал — выключить»** — бесплатно, полностью останавливает будильник
- Звук и вибрация продолжаются до нажатия одного из действий

#### 2.2.2. Логика «Поспать ещё» и нулевой баланс *(обновлено 2026-06-12 по дизайн-канону)*
1. Пользователь нажимает «Поспать ещё»
2. Система проверяет **локальный баланс** (UserDefaults/SharedPreferences)
3. Если баланс >= цены откладывания:
   - Списание записывается локально
   - Будильник откладывается на заданное время
4. Если баланс < цены откладывания — **состояние «баланс закончился» (V3)**, а не просто серая кнопка:
   - Будильник продолжает звонить
   - Карточка «Поспать ещё» отключена (desaturated, без glow), подпись «Недостаточно средств»
   - Красная пилюля «Баланса не осталось» + текст «Откладывать больше не получится. Только встать.»
   - Зелёная CTA **«Пополнить · 500 ₽»** (иконка кошелька, без Apple Pay branding) — пополнение прямо во время звонка
   - Стеклянная кнопка «Я встал — выключить»
   - (chat1.md:755, chat6.md:342-365; реализовано частично на базе V2, доделка V3 в #227)
5. Через заданное время будильник звонит снова

**⚠️ Важно:** Списание происходит **мгновенно** и **локально**. Нет зависимости от интернета.

#### 2.2.3. Прогрессивная шкала *(обновлено 2026-06-12 по дизайн-канону)*
Если пользователь включил прогрессивную шкалу для будильника — **4 ступени с потолком base×8**:

| Откладывание | Множитель | Пример (база 50 ₽) |
|--------------|-----------|---------------------|
| 1-е          | x1        | 50 ₽                |
| 2-е          | x2        | 100 ₽               |
| 3-е          | x4        | 200 ₽               |
| 4-е и далее  | x8 (потолок) | 400 ₽            |

На потолке цена больше не растёт; UI показывает подсказку «максимум — дальше только встать» (SPDawnV3.jsx:242). Реализовано в #298. CTA остаётся золотой на всех ступенях, эскалация передаётся фоном и индикатором ступеней.

Шкала сбрасывается при каждом новом срабатывании будильника (на следующий день).

#### 2.2.4. Логика кнопки «Я встал — выключить» *(обновлено 2026-06-12 по дизайн-канону)*
В MVP: простое нажатие кнопки выключает будильник. Бесплатно, без дополнительных условий. Целевое поведение V3: после «Я встал» показывается утренний summary-экран WokeMorning (#228).

**Будущее развитие (V2):** добавление заданий для выключения (решить задачку, сфотографировать QR-код).

### 2.3. Баланс и платежи

#### 2.3.1. Внутренний баланс (MVP — локальное хранение)
- У каждого пользователя есть внутренний баланс в рублях
- Баланс отображается на главном экране приложения
- **Баланс хранится локально:**
  - **iOS:** UserDefaults (`balance` key, Double)
  - **Android:** SharedPreferences (`user_balance`, Float)
- При откладывании сумма списывается из локального баланса
- **Нет синхронизации между устройствами** в MVP (фича для V2)

#### 2.3.2. Пополнение баланса (через IAP) *(обновлено 2026-06-12 по дизайн-канону)*
Пополнение реализуется через **Consumable In-App Purchase** (расходуемые покупки):

**iOS (StoreKit 2):**
- Пользователь покупает пакеты баланса
- **Лайнап пакетов:**
  - *Текущий каталог (реализовано):* 49 ₽, 149 ₽ (популярно, дефолт), 299 ₽, 499 ₽, 999 ₽
  - *Целевой лайнап по канону (chat6.md:119-152):* 250 ₽ / 500 ₽ (популярно) / 1000 ₽ — сетка как на онбординге шага 3
  - **Финальный лайнап решается в #240** — до его закрытия оба состояния зафиксированы здесь; суммы на плитках обязаны совпадать с реальными ценами SKU
- Единственное место выбора суммы — bottom sheet с пресетами (chat1.md:760-762); отдельные full-screen пикеры суммы — legacy
- После успешной покупки: баланс увеличивается в UserDefaults
- StoreKit 2 автоматически защищает от дублирования транзакций
- **Нет backend верификации в MVP** — полагаемся на StoreKit 2

**Android (Google Play Billing):**
- Аналогично через Billing Library 5+
- Consumable products
- После acknowledge() — баланс увеличивается в SharedPreferences

**Комиссия сторов:** 15% (для разработчиков < $1M/год) или 30%

#### 2.3.3. История транзакций (локальная)
- **iOS:** Core Data или SQLite
- **Android:** Room database
- Таблица transactions:
  - `id` (UUID)
  - `type` (topup / charge)
  - `amount` (Double/Float)
  - `alarm_id` (String, опционально)
  - `created_at` (Date/Timestamp)
- Список всех списаний/пополнений доступен в настройках

### 2.4. Статистика и мотивация *(обновлено 2026-06-12 по дизайн-канону)*

#### 2.4.1. Экран статистики — только поведенческая (V3)
Денежные блоки и «время подъёма» **убраны из статистики** — они либо дублируют Кошелёк, либо данные сложно получить (chat4.md:262-268). Реализовано. Состав:
- **Серия (streak)** — главный мотивационный элемент, hero-блок
- **Heatmap** — календарная тепловая карта откладываний
- **График по дням недели** — в какой день пользователь чаще всего откладывает
- **Динамика откладываний** — тренд за 8 недель: становится лучше или хуже

Никаких сумм, расходов и «чашек кофе» на этом экране. Денежная аналитика живёт в табе «Кошелёк» (недельный график трат + история транзакций).

**Источник данных:** локальная база транзакций + события пробуждений.

#### 2.4.2. Серии без откладывания
- Счётчик дней подряд без откладываний (streak)
- Лучший streak за всё время
- Простое поздравление при достижении 7, 14, 30 дней
- Streak де-монетизирован: без денежных наград за серию (#236)

### 2.5. Звуки будильника
- Набор из 8–12 предустановленных бесплатных звуков (royalty-free)
- **iOS:** `.caf` или `.aiff` файлы в bundle (до 30 секунд для уведомлений)
- **Android:** MP3/OGG файлы в `res/raw/`
- Предпрослушивание звука при выборе
- **Будущее (V2):** загрузка собственных звуков

---

## 3. Техническая архитектура (MVP — Offline-first)

### 3.1. Обзор архитектуры MVP

| Компонент | Технологии | Ответственность |
|-----------|------------|-----------------|
| **iOS-клиент** | Swift, UIKit, StoreKit 2, AlarmKit / UNUserNotificationCenter | UI, логика будильника, IAP, локальное хранение |
| **Android-клиент** | Kotlin, Jetpack Compose, Google Play Billing, AlarmManager | UI, логика будильника, IAP, локальное хранение |
| **Backend** | ❌ Нет в MVP | — |
| **Хранилище** | UserDefaults/SharedPreferences + Core Data/Room | Баланс, будильники, транзакции |

**Принцип:** приложение полностью автономно. Работает без интернета.

### 3.2. Надёжность будильника (критический раздел)

#### 3.2.1. iOS — ограничения и решения

**Проблемы:**
- Local Notifications ограничены 30 секундами звука
- Нельзя переопределить беззвучный режим и «Не беспокоить»
- Приложение не просыпается при фоновом уведомлении без действия пользователя

**Решение — двойная стратегия:**

**Стратегия A (iOS 26+, AlarmKit) — приоритетная:**
- AlarmKit позволяет создавать полноценные системные будильники
- Работает на уровне системы, как Clock.app
- Поддержка: полноэкранный экран на Lock Screen, Dynamic Island, Apple Watch
- Звонит даже в беззвучном режиме
- **Минимальная версия:** iOS 26

**Стратегия B (iOS 18–25, Local Notifications) — фолбэк:**
- `UNUserNotificationCenter` с Time Sensitive флагом
- Планирование серии уведомлений
- Действия в уведомлении: «Я встал» и «Поспать ещё (−X ₽)»
- **Ограничения:** может не сработать в беззвучном режиме

**Рекомендация:** в маркетинге указать iOS 26+ для полноценной работы.

#### 3.2.2. Android — реализация
- `AlarmManager` с EXACT алармами (`setExactAndAllowWhileIdle`)
- Foreground Service для воспроизведения звука
- Full-screen Intent для экрана будильника поверх Lock Screen
- Запрос разрешения `SCHEDULE_EXACT_ALARM` (Android 12+)
- Battery optimization whitelist

**На Android ограничения менее жёсткие — будильник работает надёжно.**

### 3.3. Локальное хранилище (MVP)

#### 3.3.1. iOS (UserDefaults + Core Data)

**UserDefaults:**
```swift
// Хранение баланса
UserDefaults.standard.set(500.0, forKey: "user_balance")
let balance = UserDefaults.standard.double(forKey: "user_balance")
```

**Core Data (транзакции):**
- Entity `Transaction`:
  - `id: UUID`
  - `type: String` (topup / charge)
  - `amount: Double`
  - `alarmID: String?`
  - `createdAt: Date`

**Core Data (будильники):**
- Entity `Alarm`:
  - `id: UUID`
  - `time: Date`
  - `repeatDays: [Int]` (0=Пн, 6=Вс)
  - `name: String`
  - `soundID: String`
  - `vibrationEnabled: Bool`
  - `snoozeMinutes: Int`
  - `penaltyAmount: Double`
  - `progressiveScale: Bool`
  - `enabled: Bool`

#### 3.3.2. Android (SharedPreferences + Room)

**SharedPreferences:**
```kotlin
// Хранение баланса
sharedPreferences.edit().putFloat("user_balance", 500f).apply()
val balance = sharedPreferences.getFloat("user_balance", 0f)
```

**Room Database (транзакции и будильники):**
```kotlin
@Entity
data class Transaction(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val type: String, // "topup" | "charge"
    val amount: Float,
    val alarmId: String?,
    val createdAt: Long
)

@Entity
data class Alarm(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val time: Long, // millis since epoch
    val repeatDays: List<Int>,
    val name: String,
    val soundId: String,
    val vibrationEnabled: Boolean,
    val snoozeMinutes: Int,
    val penaltyAmount: Float,
    val progressiveScale: Boolean,
    val enabled: Boolean
)
```

### 3.4. Логика списания (offline)

Списание штрафа происходит **локально** без зависимости от сервера:

1. Будильник звонит, пользователь нажимает «Поспать ещё»
2. Приложение проверяет локальный баланс
3. Если баланс >= цены откладывания:
   - Списание баланса: `balance -= penaltyAmount`
   - Сохранение в UserDefaults/SharedPreferences
   - Запись транзакции в Core Data/Room
   - Будильник откладывается
4. Если баланс < цены откладывания:
   - Состояние «баланс закончился» с CTA «Пополнить» (см. 2.2.2)

**Нет интернета — нет проблем.** Будильник работает полностью автономно.

---

## 4. Интерфейс пользователя (UX/UI)

### 4.1. Общие принципы дизайна *(обновлено 2026-06-12 по дизайн-канону)*
- Дизайн-система V2/V3 из хэндоффа: токены `tokens.css`, компоненты `components.css`, шрифты Manrope + JetBrains Mono
- Поддержка светлой и тёмной темы (по системным настройкам)
- Крупные элементы на экране будильника (удобство при пробуждении)
- Без рекламы
- Карточка «Поспать ещё» — warn-тон (золотой), с постепенным «покраснением» фона при прогрессиве
- Денежные действия («Пополнить») — универсальный зелёный (money) во всех темах
- **Словарь:** везде «Баланс» (не «залог»), действие — «Поспать ещё N мин»; жаргон «снуз» запрещён в продакшн-копи (chat1.md:986-998)

### 4.2. Экраны приложения

#### 4.2.1. Главный экран (Список будильников) *(обновлено 2026-06-12 по дизайн-канону)*
- Верхняя панель: пилюля баланса (с кнопкой «Пополнить») + шестерёнка настроек в правом верхнем углу
- Список будильников с toggle-переключателями
- Кнопка «+» для добавления нового будильника
- **Нижняя навигация: Будильники | Кошелёк | Статистика** (chat3.md:1635-1750; реализовано). Настройки — не таб, а дочерний экран, открывается шестерёнкой с главного экрана

#### 4.2.2. Экран создания/редактирования будильника *(обновлено 2026-06-12 по дизайн-канону)*
- Поле названия — первое сверху, крупное (как в iOS Reminders)
- Блок «Подъём» — крупный clock-XL дисплей времени
- Секция «Повтор» — Никогда / Еженедельно + дни недели
- Секция «Звук» — выбор из списка с предпрослушиванием
- Секция «Тема» — 6 готовых тем + своё фото
- Секция «Вибрация» — toggle
- Секция «Цена откладывания» (слово «штраф» в дизайне не используется):
  - Свободный числовой ввод (мин 1 ₽, максимума нет) + чипы-подсказки 20/50/100/200/500 — целевое per #230
  - Toggle «Прогрессивная шкала»
  - Предпросмотр шкалы: «1-е: 50 ₽ → 2-е: 100 ₽ → 3-е: 200 ₽ → 4-е: 400 ₽ (макс.)»
- Секция «Длительность откладывания» — слайдер 1–15 минут

#### 4.2.3. Экран сработавшего будильника *(обновлено 2026-06-12 по дизайн-канону)*
- Полноэкранный, тематизированный фон (тема будильника, 6 тем + фото)
- Крупное время (96pt, моноширинный, ultralight)
- Шапка: дата + пилюля баланса; название будильника
- Карточка «Поспать ещё N мин · −X ₽» — warn-тон (золотая), с прогрессив-индикатором ступеней
- Кнопка «Я встал — выключить» — стеклянная, прозрачный фон + рамка
- **Если баланс = 0** — будильник звонит, карточка «Поспать ещё» отключена, красная пилюля «Баланса не осталось», зелёная CTA «Пополнить · 500 ₽», стеклянная «Я встал» (см. 2.2.2; доделка V3 в #227)
- Дышащий radial glow для привлечения внимания

#### 4.2.4. Экран статистики *(обновлено 2026-06-12 по дизайн-канону)*
Только поведенческая статистика (см. 2.4.1; реализовано):
- Hero-блок Streak — серия дней без откладывания
- Heatmap откладываний
- График по дням недели
- Тренд откладываний за 8 недель
- Без денежных сумм и мотивационных сообщений о тратах

#### 4.2.5. Экран пополнения баланса *(обновлено 2026-06-12 по дизайн-канону)*
- Bottom sheet с пресетами — единственное место выбора суммы
- Текущий каталог: 49 ₽ | 149 ₽ | 299 ₽ | 499 ₽ | 999 ₽; целевой лайнап 250 / 500 (популярно) / 1000 — финал в #240 (см. 2.3.2)
- Нажатие вызывает стандартный диалог IAP (App Store / Google Play)
- Mid-firing top-up sheet (выбор суммы во время звонка) — V2 de-scope, pending #243; во время звонка — пополнение дефолтной суммой одной кнопкой

#### 4.2.6. Экран настроек *(обновлено 2026-06-12 по дизайн-канону)*
Дочерний экран (открывается шестерёнкой с главного экрана), не таб:
- Финансы: цена откладывания по умолчанию, длительность откладывания
- Звук и уведомления: громкость, вибрация
- История транзакций
- О приложении
- Политика конфиденциальности
- Пользовательское соглашение
- Связаться с разработчиком

~~Глобальное время откладывания~~ — убрано, длительность per-alarm (см. 2.1.4).

**⚠️ Убрано из MVP:**
- ❌ Авторизация/логин
- ❌ Синхронизация устройств
- ❌ Профиль пользователя

---

## 5. Бизнес-модель

### 5.1. Модель монетизации

| Источник дохода | Описание | Этап |
|-----------------|----------|------|
| Штрафы за откладывание | Основной доход. Средства с баланса пользователей (за вычетом комиссий сторов) | MVP |
| Благотворительность | Опция перечисления штрафов на благотворительность (мотивация для пользователей) | V2 |
| Премиум-функции | Расширенная статистика, кастомные звуки, задания для выключения | V3 (опционально) |

### 5.2. Экономика

Приложение **бесплатное** для скачивания. Доход формируется только от штрафов за откладывание.

**Пример расчёта (для 1000 активных пользователей):**
- Среднее пополнение: 200 ₽/месяц на пользователя
- Валовый доход: 200 000 ₽/месяц
- Комиссия стора (15%): -30 000 ₽
- **Серверные расходы:** 0 ₽ (нет backend в MVP)
- **Чистый доход:** ~170 000 ₽/месяц

**Примечание:** это оптимистичный сценарий. Реальные цифры зависят от retention и среднего чека.

### 5.3. One-way stake — невозвратная ставка *(добавлено 2026-06-12 по дизайн-канону)*

Продукт — **commitment device**: депозит работает как невозвратная ставка на собственную дисциплину (chat3.md:196-269).

- **Вывода средств нет и не будет.** Экран «Вывести с баланса» удалён из дизайна; любые точки входа («Вывести», «Вернуть деньги», «Снять») запрещены. Ограничение продиктовано и StoreKit/Play Billing (one-way IAP), и требованиями финансового лицензирования
- Деньги с баланса **тратятся только** на цену откладывания
- **Дисклеймеры обязательны** на экранах депозита, онбординга (шаг 3) и в кошельке: «Покупка не возвращается · списывается только при откладывании». Копирайтинг, намекающий на возвратность («вернуть можно в любой момент»), запрещён
- Удаление будильника **не трогает баланс** — баланс привязан к аккаунту, не к будильнику
- Бонусные зачисления (если появятся) — one-way кредиты, не «возвраты»

**V2 de-scope (pending #243):** referral-программа, care-screen, экран «Способы оплаты», mid-firing top-up sheet с выбором суммы — вырезаны из MVP, в коде не должны участвовать в экономике.

---

## 6. Юридические и этические вопросы

### 6.1. Необходимые документы
- Политика конфиденциальности (Privacy Policy) — обязательна для App Store и Google Play
- Пользовательское соглашение (Terms of Use) — описание механики штрафов
- Согласие на списание средств — при первом использовании функции откладывания
- Все документы должны быть доступны внутри приложения и по ссылке (для модерации)

### 6.2. Ключевые юридические положения
- Чёткое описание: деньги списываются при нажатии кнопки «Поспать ещё»
- Пользователь самостоятельно пополняет баланс и осознаёт, что средства списываются безвозвратно
- Разработчик не несёт ответственности за опоздания из-за технических сбоев
- Disclaimer о том, что будильник на iOS < 26 может не сработать в беззвучном режиме

### 6.3. Возврат средств *(обновлено 2026-06-12 по дизайн-канону)*
- Стандартный возврат через App Store / Google Play (по правилам сторов)
- В пользовательском соглашении: депозит — невозвратная ставка (one-way stake, см. 5.3); списания за откладывание добровольны и не подлежат возврату
- Внутри приложения механизма вывода/возврата нет
- **Механизм подтверждения (опционально):** краткий таймер (3 секунды) перед списанием

### 6.4. Правила App Store и Google Play
- Штрафы реализуются как расходование внутреннего баланса (пополняется через IAP) — это легитимная модель
- Не является азартными играми (нет случайного результата)
- Не является подпиской (разовые пополнения)
- Необходимо пройти ревью и возможно дать пояснения модераторам

---

## 7. MVP и этапы разработки

### 7.1. MVP (Версия 1.0) — Offline-first

Минимально жизнеспособный продукт:

*(таблица обновлена 2026-06-12 по дизайн-канону)*

| Функция | Приоритет | Описание |
|---------|-----------|----------|
| Создание/редактирование будильников | Критичный | Время, повтор (Никогда/Еженедельно), название, звук, тема, вибрация |
| Список будильников | Критичный | С toggle, удалением, редактированием |
| Экран сработавшего будильника | Критичный | «Я встал» + «Поспать ещё» (платно), 6 тем |
| Внутренний баланс (локально) | Критичный | UserDefaults/SharedPreferences, one-way stake |
| Пополнение через IAP | Критичный | Bottom sheet с пресетами, StoreKit 2 / Google Play Billing (лайнап — #240) |
| Прогрессивная шкала | Критичный | Включение/выключение, ×2 с потолком ×8 (4 ступени) |
| Цена откладывания | Критичный | Мин 1 ₽, без максимума, индивидуально для каждого будильника (#230) |
| Поведенческая статистика (локально) | Важный | Streak, heatmap, дни недели, тренд 8 недель |
| Настройки | Важный | Дочерний экран с шестерёнки, юрид. документы |
| AlarmKit (iOS 26+) | Критичный | Полноценный будильник на системном уровне |
| Local Notifications (iOS <26) | Важный | Фолбэк для старых версий iOS |
| Звуки будильника | Важный | Предустановленные бесплатные звуки |

**⚠️ Убрано из MVP:**
- ❌ Backend сервер
- ❌ Авторизация/логин
- ❌ API эндпоинты
- ❌ Синхронизация между устройствами
- ❌ Облачный бэкап

### 7.2. Версия 2.0 (Cloud & Social)

**Добавляется:**
- ✅ **Backend сервер:** Node.js/Python + PostgreSQL
- ✅ **Авторизация:** device_id или email
- ✅ **Синхронизация баланса** между устройствами
- ✅ **Облачный бэкап будильников**
- ✅ **Благотворительность:** выбор фонда для перечисления штрафов
- ✅ **Задания для выключения:** задачки, QR-код
- ✅ **Расширенная статистика** и графики
- ✅ **Streak-система** с достижениями

### 7.3. Версия 3.0 (Premium features)

**Опционально:**
- Загрузка своих звуков будильника
- Социальные функции (соревнования с друзьями)
- Интеграция с Apple Health / Google Fit
- Виджеты для домашнего экрана
- Премиум-подписка (расширенные фичи)

---

## 8. Figma дизайн

**Ссылка:** https://www.figma.com/design/DU3IMT4uEiw8rua0r3uy1D/

**Требуется доработка:**
- ❌ Убрать экраны авторизации/логина
- ✅ Добавить экран пополнения баланса (IAP)
- ✅ Экран статистики (локальная, без облачной синхронизации)

---

## 9. Roadmap

| Этап | Сроки | Фокус |
|------|-------|-------|
| **Фаза 0** | 1-2 дня | Аудит и доработка Figma-дизайна |
| **MVP (v1.0)** | 5-7 недель | Offline-first, IAP, будильники, статистика |
| **Версия 2.0** | +6-8 недель | Backend, синхронизация, благотворительность |
| **Версия 3.0** | TBD | Premium features, социальные фичи |

---

**Конец ТЗ v2.1**
